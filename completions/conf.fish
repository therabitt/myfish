# Tab completions for the 'conf' function
complete -c conf -f
complete -c conf -a fish -d "Fish shell config"
complete -c conf -a hypr -d "Hyprland config"
complete -c conf -a var  -d "Hyprland variables"
complete -c conf -a key  -d "Hyprland keybinds"
complete -c conf -a rul  -d "Hyprland rules"
complete -c conf -a cael -d "Caelestia shell"
complete -c conf -a star -d "Starship prompt"
